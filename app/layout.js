import './globals.css';

export const metadata = {
  title: 'Ges Cobranza — Control de Facturas',
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
